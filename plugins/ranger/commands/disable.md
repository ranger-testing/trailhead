---
description: "Disable Ranger hooks for this session by running `ranger hook disable`"
allowed-tools: ["Bash(ranger hook disable)"]
---

# Disable Ranger

RUN `ranger hook disable`

After running, do NOT use /ranger or any `ranger` CLI commands for the rest of this session.
